/**
 * School site configuration.
 * Safe to import in both Server and Client Components (no server-only APIs).
 */

/** IANA timezone for the school. Used for all date/time formatting. */
export const SITE_TIMEZONE = 'America/Chicago';
